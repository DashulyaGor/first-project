#include <stdio.h>

int main(){
    /*
    char array[256];
    int x, y;
    scanf("%d%d", &x, &y);
    if (y == x) {
    // make a positive signal
    } else {
    // make a negative signal
    }

    input string if next string is equale
    reading code
    finding in the array the matchs
    makes a signal
    create a visual interface
    pack it all in a container
    connect these threads to execute the code
    */
}